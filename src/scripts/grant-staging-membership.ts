import { runStagingGrantMembershipCli } from '../membership/run-staging-grant-membership.js';

// Compiled production entrypoint: `node dist/scripts/grant-staging-membership.js`
// npm run membership:grant:staging:production -- --account-id <uuid>
runStagingGrantMembershipCli();
